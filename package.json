{
  "name": "blackhole-server",
  "version": "1.0.0",
  "description": "Blackhole video downloader backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "pip install yt-dlp || pip3 install yt-dlp || true"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
